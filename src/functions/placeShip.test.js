import { BlankBoard, placeShip } from "./functions";
import { ShipTypes } from "../assets/index";

test("Testing ship placement in to grid", () => {
  // let board = BlankBoard();
  // board = placeShip(board, 10, ShipTypes.small.model, "5", 6);
  // // console.log(board);
  // const expectedboard = [...board];
  // expectedboard[17] = { id: "5", type: "lod" };
  // expectedboard[27] = { id: "5", type: "lod" };
  // expect(board).toEqual(expectedboard);
});
